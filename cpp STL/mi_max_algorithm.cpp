#include <iostream>
#include <algorithm>
#include <vector>
using namespace std ;


int main () {
 int a = 25;
 int b = 32;
//  cout << max(a,b) << endl; find the maximum element
//  cout << min(a,b) << endl; find the minimum element

//min max in range 
    vector<int> arr(5);
    arr[0] = 10;
    arr[1] = 20;
    arr[2] = 30;
    arr[3] = 40;
    // arr[4] = 50;
    // auto it = min_element(arr.begin(),arr.end());
    // cout << *it << endl;
    // auto it = max_element(arr.begin(),arr.end());
    // cout << *it << endl;


}